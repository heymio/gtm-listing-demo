# Asset integrity

Final hardening binds exact files by SHA-256, role, approved scope, and asset-to-slot contract. A same-name replacement does not inherit approval when bytes change.

Material crop, recomposition, role change, or other transform requires explicit authorization. Exact physical identity and semantic role are separate checks.
