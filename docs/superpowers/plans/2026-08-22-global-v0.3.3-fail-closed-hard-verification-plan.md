# Global v0.3.3 Implementation Plan

1. Port Japan v0.3.3 adversarial hardening cases to market/channel-neutral Global fixtures and wire them into CI (RED).
2. Upgrade Production required-set and Freeze contracts; preserve Global profile inputs (GREEN).
3. Upgrade Hardening Delivery State to fail-closed union/readiness/frontend/runtime gates (GREEN).
4. Upgrade Evidence Auditor to real decoder verification and PROOF_VISUAL claim/source binding (GREEN).
5. Upgrade standalone Demo preflight and add real Playwright/Chromium no-network runtime validation (GREEN).
6. Upgrade deterministic/symlink-safe/self-validating package builders and add reproducibility tests (GREEN).
7. Add exact-SHA release workflow with least privilege and PR read-only validation mode; add distribution/release regressions.
8. Update VERSION/CHANGELOG/README/install/contracts/templates and keep Global profile mirrors unchanged.
9. Run final fresh CI on exact head, review diff/leakage/profile mirrors, and leave a Draft PR for explicit merge approval.
